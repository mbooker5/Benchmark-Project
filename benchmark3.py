import time

start = time.time()

required_length = (5 * (10 ** 9))
array = [0] * required_length

for ind,num in enumerate(required_length):
    # read array items
    zero = num

    #write array items
    array[ind] = ind


    

